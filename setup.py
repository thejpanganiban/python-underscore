from setuptools import setup, find_packages


setup(name="Underscore",
      description="Underscore port in Python",
      version="0.1.0",
      packages=find_packages(),
      install_requires=[],
      test_suite='tests')
